{
  "properties": {
    "MAX_TOKENS": {
      "default": "20000",
      "description": "Maximum tokens per response",
      "type": "string"
    },
    "NODE_ENV": {
      "default": "production",
      "description": "The Node.js environment setting",
      "type": "string"
    },
    "OBSIDIAN_SHEET_PLUS_API_KEY": {
      "description": "Your API key for the Obsidian Sheet Plus MCP Server",
      "type": "string"
    },
    "OBSIDIAN_SHEET_PLUS_BASE_URL": {
      "default": "http://127.0.0.1:27123",
      "description": "The base URL of your Obsidian Sheet Plus plugin",
      "type": "string"
    },
    "OBSIDIAN_SHEET_PLUS_VERIFY_SSL": {
      "default": "false",
      "description": "Enable SSL verification for Obsidian Sheet Plus API",
      "type": "string"
    },
    "RATE_LIMIT_MAX_REQUESTS": {
      "default": "200",
      "description": "Max requests per rate limit window",
      "type": "string"
    },
    "RATE_LIMIT_WINDOW_MS": {
      "default": "900000",
      "description": "Rate limit window in milliseconds (default: 15 minutes)",
      "type": "string"
    },
    "TOOL_TIMEOUT_MS": {
      "default": "60000",
      "description": "Tool execution timeout in milliseconds",
      "type": "string"
    }
  },
  "required": ["OBSIDIAN_SHEET_PLUS_API_KEY", "OBSIDIAN_SHEET_PLUS_BASE_URL"],
  "type": "object"
}
